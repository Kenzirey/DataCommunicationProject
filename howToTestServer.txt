Used git bash (can use run too I guess?)

find directory of the file:
my directory is this, so I will use this for my own testing, should add your own file path:
- cd "C:\Users\Alerc\OneDrive\Skrivebord\3 Semester\Data Communication\Project\DataCommunicationProject"

- mvn clean compile

run the commands with this:
- mvn exec:java -Dexec.mainClass="no.ntnu.server.CommandLineTestClient" -Dexec.args="echo Hello Server!"

Can use your own commands:
mvn exec:java -Dexec.mainClass="no.ntnu.server.CommandLineTestClient" -Dexec.args="COMMAND GOES HERE!!"
* instead of COMMAND GOES HERE for example can use "name" to get server's name, or "time" to get current time.
for example:
mvn exec:java -Dexec.mainClass="no.ntnu.server.CommandLineTestClient" -Dexec.args="name"

